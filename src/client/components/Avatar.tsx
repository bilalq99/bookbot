// PLACEHOLDER — owned by agent C1 (client-shell). Replace entirely; keep the
// default export and props EXACTLY (C2/C3 compile against them).
// Initials on a deterministic gradient derived from the username hash.
export interface AvatarProps {
  username: string
  displayName?: string
  size?: number // px, default 40
}

export default function Avatar(_props: AvatarProps) {
  return null
}
