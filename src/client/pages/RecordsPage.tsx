// PLACEHOLDER — owned by agent C3 (client-log-profile). Replace entirely; keep the
// default export. Own trophy room — docs/SPEC.md §10: marquee strip (best
// Squat/Bench/Deadlift max_weight + TOTAL when all three exist; est. total from
// max_est_1rm labeled "est."), then PRs grouped by exercise with record chips +
// dates (api.userPrs(me)). Empty state: "Chalk up your first session." Prefix: .rec-
export default function RecordsPage() {
  return null
}
