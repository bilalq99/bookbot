// PLACEHOLDER — owned by agent C1 (client-shell). Replace entirely; keep the
// default export. Edit display name + bio, kg/lb toggle (api.patchMe -> setUser),
// Logout button. Class prefix: .auth- or .shell- as fits.
export default function SettingsPage() {
  return null
}
