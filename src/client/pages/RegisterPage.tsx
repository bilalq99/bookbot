// PLACEHOLDER — owned by agent C1 (client-shell). Replace entirely; keep the
// default export. Username (lowercase live-normalized), display name, password,
// kg/lb toggle; inline errors; link to /login. Redirect authed users to /.
export default function RegisterPage() {
  return null
}
