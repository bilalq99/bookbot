// PLACEHOLDER — owned by agent C1 (client-shell). Replace entirely; keep the
// default export. Centered dark card, CHALK wordmark, username/password, inline
// errors, link to /register. Redirect authed users to /. Class prefix: .auth-
export default function LoginPage() {
  return null
}
