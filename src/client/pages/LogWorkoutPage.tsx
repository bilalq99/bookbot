// PLACEHOLDER — owned by agent C3 (client-log-profile). Replace entirely; keep the
// default export. The /log flow — docs/SPEC.md §10:
// start screen (Start empty · Repeat last session) unless a localStorage draft
// exists (key 'chalk.activeSession' -> resume banner); active editor (editable
// title, elapsed timer, exercise blocks w/ SetEditor, ExercisePicker, RestTimer,
// every edit -> localStorage); Finish -> compose (auto title from dominant muscles
// + weekday, notes, up to 4 photos via api.uploadMedia, then api.createWorkout +
// api.publishWorkout, celebrate newPrs, navigate to /s/:id, clear localStorage).
// Discard w/ confirm. Prefix: .log-
export default function LogWorkoutPage() {
  return null
}
