// PLACEHOLDER — owned by agent C2 (client-feed). Replace entirely; keep the
// default export. Segmented Following · Everyone (default Following), FeedItem
// list, IntersectionObserver infinite scroll on nextCursor, pull of fresh data on
// scope switch, empty state nudging to Everyone + /discover. Prefix: .feed-
export default function FeedPage() {
  return null
}
