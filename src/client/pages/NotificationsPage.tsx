// PLACEHOLDER — owned by agent C3 (client-log-profile). Replace entirely; keep the
// default export. Reverse-chron list w/ cursor paging; unread rows get a plate-red
// left rule; copy per type (bump/comment/follow/pr — docs/SPEC.md §10); tap ->
// session or profile; "Mark all read". Prefix: .ntf-
export default function NotificationsPage() {
  return null
}
