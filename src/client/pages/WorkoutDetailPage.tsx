// PLACEHOLDER — owned by agent C2 (client-feed). Replace entirely; keep the
// default export. Route /s/:id — hero (carousel/SessionCard), title, PR chips,
// date + duration, stat tiles (Volume/Sets/Top set/Duration), per-exercise set
// tables (columns per metricType, warmups dimmed "W", est.1RM dim column, PR rows
// flagged), bumps row (avatar stack -> likers sheet), CommentList, owner ⋯ menu
// (edit title/notes inline, delete w/ confirm -> navigate home). Prefix: .wk-
export default function WorkoutDetailPage() {
  return null
}
