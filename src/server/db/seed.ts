// PLACEHOLDER — owned by agent T1 (tests-seed). Replace entirely.
// npm run seed — docs/SPEC.md §13: deterministic (mulberry32, fixed base timestamp),
// refuses to run when users exist unless --force (then deletes DB + uploads);
// 9 users (demo/demo1234 + 8 athletes, password chalk1234), follow graph,
// ~70 published workouts over 6 weeks THROUGH createWorkout/publishWorkout,
// likes + comments with counters/notifications correct; prints a login table.
console.error('seed not implemented yet')
process.exit(1)
